# Todo

- Implement the rest of the string functions
- Instead of storing the length of a string on the heap, strings should be a (length, pointer) tuple
- Check support for lists of ints, strs, classes, etc.